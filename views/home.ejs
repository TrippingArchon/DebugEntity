


<%- include("partials/header"); -%>

    <div class="grid">
        <h1>Post 1</h1>
        <p>
            <%= startingContent %>
        </p>
    </div>

    <% posts.forEach((blog)=>{ %>
        <div class="grid">
            <h1>
                <%= blog.title %>
            </h1>
            <p>
                <%= blog.content %>
            </p>
        </div>
        <% }) %>

    <%- include("partials/footer"); -%>